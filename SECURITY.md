# はじめに確認すること

教材を初めて試すときや、更新したときの簡単な確認用メモです。毎回読み込ませる必要はありません。

- [ ] 新しく用意した空のフォルダへ展開し、他の仕事のファイルと混ざっていない。
- [ ] `.exe`・`.dll`・インストーラー等の実行ファイルや関連バイナリが配布物に含まれていない。
- [ ] APIキー・パスワード・個人情報が入っていない。
- [ ] 必要なツールと、作成・変更するファイルの場所が分かる。
- [ ] 外部への送信や追加ダウンロードがある場合、その内容と接続先が分かる。
- [ ] 既存ファイルを無断で上書き・削除する処理がない。

空のフォルダは初回の展開先のことです。教材や自分で用意した素材を置いた後まで空である必要はありません。既存作品を続けるときは、その作品のフォルダを指定します。

AIに確認を頼む例：

```text
この教材をまだ実行せず、ファイルを読んで確認してください。
実行ファイルや関連バイナリ、秘密情報、外部通信、上書き・削除の処理、
作業フォルダ外への書き込みがあるか、短く報告してください。
```

確認結果が分からない場合は、その箇所を確認してから試してください。ソースが公開されていることや、一度起動できたことだけで安全が保証されるわけではありません。

このメモは実行に必要なファイルではありません。内容を確認した後は、手元の `SECURITY.md` を削除したり、AIに渡す方針ファイルから外したりして構いません。自動削除はしません。更新時に再確認できるよう、GitHubには残します。

実行時に守る入力条件・確認方法・停止条件は、各教材の3つの方針ファイルに記載します。
